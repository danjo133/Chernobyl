# Resolve bare image names (e.g. `FROM ubuntu`) against Docker Hub. Add private
# registries here as needed — and remember to allowlist their hosts in
# gateway/allowed-domains.txt, or pulls will be DENIED by the egress firewall.
unqualified-search-registries = ["docker.io"]
short-name-mode = "permissive"
